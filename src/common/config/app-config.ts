export interface AppConfig {
  WSS_ENDPOINT: string;
  PORT: string;
  PEST_COLLECTION: number;
  GUIDE_COLLECTION: number;
  WALLET_MNEMONIC: string;
  ENCRYPTION_PHRASE: string;
  DECRYPTION_PHRASE: string;
  PINATA_JWT: string;
  PINATA_GATEWAY_URL: string;
  IPFS_GATEWAY_URL: string;
}
